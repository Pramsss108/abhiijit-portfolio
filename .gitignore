node_modules/
perf-reports/
backups/
*.log
.DS_Store
Thumbs.db
.wrangler/
ai-proxy/.wrangler/

# Secrets — never commit. Tokens/keys read at runtime only, never committed.
ai-proxy/whatsapp/secret.token
ai-proxy/resend.key
*.token
*.key
.env
.env.local
.dev.vars
