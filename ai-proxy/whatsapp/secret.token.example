# HOW TO USE THIS FILE
# 1. Make a copy of this file named exactly:  secret.token   (drop ".example")
# 2. Delete these comment lines and paste ONLY your permanent WhatsApp token
#    on a single line below. Nothing else. Save.
#
# secret.token is git-ignored — it never gets committed and never leaves your PC.
# Claude's scripts read it at runtime but never print or store the value.
#
# Paste your token below (replacing this line):
EAAG...your_permanent_token_here...
